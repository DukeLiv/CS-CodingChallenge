#include <stdio.h>

void solution(int n, int flows[n])
{
    // Write solution here
}

int main()
{
    long long int n;
    scanf("%lld", &n);

    int flows[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &flows[i]);
    }

    solution(n, flows);

    return 0;
}
