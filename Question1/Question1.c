#include <stdio.h>

char* solution(long n)
{
    // Write solution here
    return "";
}

int main()
{
    long n = 0;
    scanf("%ld", &n);

    char *answer = solution(n);
    printf("%s\n", answer);

    return 0;
}
