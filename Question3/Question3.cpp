#include <iostream>

using namespace std;

int solution(int N, int M, int P[])
{
    // Write solution here
    return 0;
}

int main()
{
    int N, M;
    cin >> N >> M;

    int P[N];
    for (int i = 0; i < N; i++)
    {
        cin >> P[i];
    }

    int answer = solution(N, M, P);
    cout << answer << endl;

    return 0;
}
